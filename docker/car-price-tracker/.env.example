TIMEZONE=Europe/Zurich

# Database
POSTGRES_USER=cartracker
POSTGRES_PASSWORD=cartracker
POSTGRES_DB=cartracker

# App
BASE_URL=https://www.example.com
SCRAPE_INTERVAL_HOURS=24
RETRY_INTERVAL_HOURS=6
MAX_PAGES_PER_FILTER=200

# Optional for external Chrome DevTools Protocol providers. Leave unset to
# launch bundled Chromium directly.
# CDP_ENDPOINT=
